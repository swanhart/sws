#ifndef PLAYLIST_H
#define PLAYLIST_H


class playlist
{
    public:
        playlist();
        virtual ~playlist();
        playlist(const playlist& other);
        playlist& operator=(const playlist& other);

    protected:

    private:
};

#endif // PLAYLIST_H
